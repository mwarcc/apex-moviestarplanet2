{
  "name": "apex-moviestarplanet2",
  "version": "1.4.9",
  "description": "Advanced toolkit Chrome extension for MovieStarPlanet2",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "node build.js",
    "build:dev": "node build.js --dev",
    "clean": "node -e \"const fs=require('fs');['dist/apex.js','dist/bootstrap.js'].forEach(f=>{try{fs.unlinkSync(f)}catch{}});console.log('Cleaned dist/')\""
  },
  "devDependencies": {
    "terser": "^5.48.0"
  },
  "engines": {
    "node": ">=16"
  }
}
